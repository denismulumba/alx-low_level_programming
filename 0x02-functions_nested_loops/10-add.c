#include "main.h"
/**
 * add -adds two integers and returns the result.
 * @a: furst integer to add
 * @b: second integer to add
 *
 * Return: sum of the two integers
 */
int add (int a, int b)
{
	return (a + b);
}
